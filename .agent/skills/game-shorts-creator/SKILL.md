---
name: game-shorts-creator
description: >-
  Пайплайн создания коротких вертикальных видеороликов (Shorts/TikTok) про баги в видеоиграх.
  Используется для скачивания фрагментов YouTube, генерации ироничного сценария, создания
  крупных 2D-схем, наложения мемов, генерации TTS и финального монтажа в FFmpeg.
---

# Onter's inn Shorts Creator Skill

Руководство и алгоритм создания новых выпусков для автоматизированного канала про игровые баги.

## Пошаговый пайплайн производства (3-Step Production Pipeline):

### 1. Этап 1: Получение вводных, нарезка геймплея и разбор бага
- **Вход от пользователя:** Ссылка на видео (YouTube / локальный файл), таймкоды и **1 предложение с прямой фактурой сутевой причины бага**.
  - *Правило ввода фактуры:* Пользователь дает 1 четкое предложение про суть бага (например: *"Грузовик едет по ИИ-сплайну и на перекрестке моментально поворачивает на 90°, сшибая игрока"*). Агент берет этот факт за незыблемый базис.
- **Действия агента:**
  - Скачивание видео strictly на **диск E:** (`assets/downloads/` или `output/<id>/temp/`). Использование диска C запрещено.
  - Нарезка фрагмента геймплея (5–10 сек) для фонового зацикливания.
  - **Frame Sampling Check (Обязательный контроль среза):** Извлечение 3 кадров (`0-2s`, `середина`, `последние 2s`), проверка отсутствия обрезков прошлых видео из компиляций и черных экранов.
  - **Превью отрезка в MP4:** Черновой вертикальный клип для подтверждения пользователем.
- **Контрольная точка 1:** Пользователь в чате подтверждает отрезок геймплея и суть бага.

### 2. Этап 2: Сценарий, раскадровка и Компактные Умные Карточки — 3 ВАРИАНТА
- **Создание 3 различных вариантов выпуска (Variant 1, Variant 2, Variant 3):**
  - Сразу 3 разных варианта сценария с отличающейся подачей, юмором и акцентами.
  - Рендеринг в подпапки (`output/<id>/variant1/video.mp4`, `variant2/video.mp4`, `variant3/video.mp4`) и копирование основного в `output/<id>/video.mp4`.

- **📐 Стандарт верстки Компактных Карточек (`SmartCardGenerator`):**
  - **Контейнер 1/3 высоты ($1000 \times 600\text{ px}$):** Схемы рендерятся в компактном блоке на 1/3 высоты кадра (`y=80`–`y=680`), **полностью заполняя пространство без дыр и пустот**.
  - **Разделение на 2 простые карточки (Split 2-Card Flow):** Категорически запрещено перегружать 1 кадр кучей текста. Схема разбивается на **2 последовательные легкие карточки**:
    - *Карточка 1 (Step 1/2):* Исходное состояние и 3D-мир.
    - *Карточка 2 (Step 2/2):* Ошибка движка и сбой стейт-машины.
  - **💻 Стиль ОС Window IDE для карточек кода:** Карточки кода рендерятся в виде **окна редактора ОС** (кнопки управления `🔴🟡🟢`, имя файла `RigidBodySolver.cpp`, блоки `[-] WRONG` vs `[+] FIXED`).
  - **🚫 Строгий 100% запрет на эмодзи (Strict Emoji Ban):** Категорически запрещено использовать любые эмодзи-символы во всех текстовых строках схем и карточек. Использовать строго чистые текстовые маркеры `[-]`, `[+]`, `[!]`, `[INFO]`, `*`, `//`, `->`.

- **🎭 Стандарт диалога двух ведущих (Dual-Voice Dialogue Format):**
  - 👩 **Ведущая / Диктор (`host`) — Светлана (`ru-RU-SvetlanaNeural` / `en-US-AnaNeural`):**
    - Озвучивает **Хук (Hook)**, эмоциональную завязку, вопросы и **Outro (Финал)** с призывом подписаться на **Onter's inn** и просить обьяснить проще.
  - 👨 **Инженер / Эксперт (`expert`) — Дмитрий (`ru-RU-DmitryNeural` / `en-US-ChristopherNeural`):**
    - Озвучивает **Схемы 1, 2 и Карточки кода**.
    - Объясняет сухую математику, векторы физического движка, ошибки в сплайнах и коде.

- **ОБЯЗАТЕЛЬНЫЙ блок Outro и плашка Outro Motion Video:**
  - Дикторский текст блока `outro` (озвучивает Ведущая) **ОБЯЗАТЕЛЬНО** содержит название канала (**Onter's inn**), призыв **подписаться**, поставить **лайк** и оставить **комментарий**.
  - Наложение плашки `output/templates/outro_subscribe_motion.mp4` со сдвигом `setpts`, хромакеем `chromakey=0x00FF00:0.28:0.15` и родным звуком клика.

- **Подбор мемов:** Чистые гифки-реакции без рамок и подложек (`TenorMemeFetcher`) + SFX с MyInstants (`MyInstantsSFXFetcher`, с мягким уровнем громкости).

### 3. Этап 3: Единый рендер через Ядро (`PipelineRunner`) и Авто-Валидация
- **Сборка strictly через `src/core/pipeline_runner.py`:**
  - **Запрет одноразовых скриптов:** Рендеринг выполняется строго вызовом центрального ядра `PipelineRunner(episode_id)`. Генерировать кастомные `generate_epXX_full_production.py` с адовыми костылями запрещено.
- **⛔ Strict No-Polling Rule:** При запуске асинхронных или командных задач рендера (`run_command` с `IsDaemon=false`), агенту КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО вызывать `manage_task(Action="status")` в цикле. Агент должен завершить ход или переключиться на другие задачи, ожидания автоматического `SYSTEM_MESSAGE`.
- **🔍 4-Point Mandatory Smoke Test (Авто-Валидация):**
  1. *Decodability Test:* `ffprobe` декод-тест всех `.mp4` файлов.
  2. *Dual-Voice Assert:* Проверка наличия аудиофайлов Светланы и Дмитрия.
  3. *Visual Overlay Assert:* Извлечение кадров и контроль оверлея схем в `y=80`–`y=680`.
  4. *Metadata Format Assert:* Контроль 4 секций в `description.txt`.

## Технические стандарты и модули:

### 1. Подготовка структуры вывода
Использовать `EpisodeOutputManager(episode_id)` для создания изолированной папки `output/<episode_id>/`:
- `voice/`, `subtitles/`, `visuals/`, `frames/`, `temp/`

### 2. Мультиязычность и ролевая озвучка
- `ru`: Ведущая (`host`) = `ru-RU-SvetlanaNeural`, Эксперт (`expert`) = `ru-RU-DmitryNeural`.
- `en`: Ведущая (`host`) = `en-US-AnaNeural`, Эксперт (`expert`) = `en-US-ChristopherNeural`.

### 3. Сборка видео в FFmpeg (Strict Zero-Overlap Grid)
- Зона геймплея: `y=200`–`y=774` (scaled 16:9).
- Зона компактных карточек (1/3 высоты): `y=80`–`y=680` ($1000 \times 600\text{ px}$).
- Зона мемов: `y=860`–`y=1400`.
- Зона субтитров: `y=1480`–`y=1540` (`MarginV=420`).
- Аппаратный NVENC (`h264_nvenc`).
- Фоновый эмбиент из YouTube Audio Library (громкость 0.14) + SFX (0.25).

### 4. Генерирование метаданных и description.txt
- Описание и теги ОБЯЗАТЕЛЬНО генерируются с помощью `src/core/metadata_generator.py` (`MetadataGenerator.generate()`).
- Описание содержит 4 секции: Заголовок, Полное описание, Закрепленный комментарий, Теги.

### 5. Безопасность контента (Stop-Words Blacklist)
- Использование безопасного словаря замен (`трухляк`, `мякиш`, `регдолл`, `0 HP`, `деспавн`, `финишнул`, `отправил на спавн`).
